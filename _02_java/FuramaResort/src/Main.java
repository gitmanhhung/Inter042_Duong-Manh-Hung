import models.Room;
import models.Villa;

import java.util.List;
import java.util.Scanner;
import java.util.regex.Matcher;
import java.util.regex.Pattern;

public class Main {
    /*public static void main(String[] args) {
        String id = "SVVL-0001";
        String name = "R";
        float area = 31.22f;
        String patternID = "^SV+[VL|HO|RO]+[-]\\d{4}$";
        String patternName = "^[A-Z][a-z]{0,}+$";
        String patternArea = "^[3-9]{2,}[.][0-9]{2,}$";
        System.out.println(id + ": " + Pattern.matches(patternID, id));
        System.out.println(name + ": " + Pattern.matches(patternName, name));
        System.out.println(area + ": " + Pattern.matches(patternArea, area));

    }*/
}

